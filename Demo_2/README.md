This is Demonstration 2 of my End-of-study project

this part is set to be executed on a jetson nano developer kit and inside a docker container

the code triggers a buzzer and an LED so those needs to be set on their corresponding ports

after setting up the virtual environment
All you have to do is run the code reuse.py

After detecting an image (from the ones trained in best.engine) for 100 frames, the buzzer and LED are triggered
